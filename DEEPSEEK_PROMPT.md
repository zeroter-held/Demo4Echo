# 给 DeepSeek 的首条提示词

请先阅读 `DEEPSEEK_HANDOFF.md`、`CHANGED_FILES.txt` 和补丁文件，不要立即修改代码。

这是一个运行在 RV1106 上的 LVGL 320×240 桌面机器人项目，使用 ARM32 hard-float/uClibc 工具链交叉编译。当前二次开发新增了番茄钟 App，恢复了原摄像机预留入口，并把番茄钟放到独立第三页。

请先完成代码审查并输出结论：

1. 梳理番茄钟页面的初始化、运行、暂停、重置和销毁生命周期。
2. 检查 `lv_timer_t` 是否在所有退出路径正确释放，是否存在重复进入后的悬空指针或重复定时器。
3. 检查第三页 App 坐标、分页点、左右滑动边界是否正确。
4. 检查 `ui_font_focus22.c` 是否覆盖页面实际使用的全部中文字符。
5. 检查 CMake 中 ARM运行库打包和 `$ORIGIN/lib` RPATH 是否合理。
6. 不修改或输出 API Key、Wi-Fi密码、SSH私钥和真实访问令牌。
7. 不覆盖开发板 `/root/bin/system_para.conf`。
8. 每一步必须解释：做什么、为什么、命令含义、成功标准、失败含义。

先只审查并列出风险，得到确认后再修改。
